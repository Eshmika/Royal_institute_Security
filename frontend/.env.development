# Ensure CRA dev server uses localhost to avoid embedding private IPs
HOST=localhost
WDS_SOCKET_HOST=localhost
# If you run on a non-default port, CRA sets PORT automatically; you can force:
# PORT=3000
